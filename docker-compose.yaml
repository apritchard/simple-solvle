# Local dev: builds and runs the single combined container (Spring Boot serving
# the React build) — same image that ships to Lightsail. Open http://localhost:8081
services:
  solvle:
    build: .
    image: solvle:local
    container_name: solvle
    ports:
      - "8081:8081"
